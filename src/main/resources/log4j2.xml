<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="LOG_LEVEL">INFO</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- File Appender for general logs -->
        <File name="FileAppender" fileName="logs/docix.log">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </File>

        <!-- Rolling File Appender for API access logs -->
        <RollingFile name="ApiAccessFile" fileName="logs/api-access.log"
                     filePattern="logs/api-access-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy />
                <SizeBasedTriggeringPolicy size="50MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- Rolling File Appender for error logs -->
        <RollingFile name="ErrorFile" fileName="logs/error.log"
                     filePattern="logs/error-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            <Policies>
                <TimeBasedTriggeringPolicy />
                <SizeBasedTriggeringPolicy size="50MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Application specific loggers -->
        <Logger name="com.example.DocIx" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileAppender"/>
            <AppenderRef ref="ErrorFile"/>
        </Logger>

        <!-- API Access Logger -->
        <Logger name="API_ACCESS" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="ApiAccessFile"/>
        </Logger>

        <!-- Spring Framework -->
        <Logger name="org.springframework" level="INFO"/>

        <!-- Elasticsearch -->
        <Logger name="org.elasticsearch" level="INFO"/>
        <Logger name="co.elastic.clients" level="INFO"/>

        <!-- RabbitMQ -->
        <Logger name="org.springframework.amqp" level="INFO"/>

        <!-- Database -->
        <Logger name="org.hibernate" level="INFO"/>
        <Logger name="org.flywaydb" level="INFO"/>

        <!-- Root logger -->
        <Root level="${LOG_LEVEL}">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileAppender"/>
            <AppenderRef ref="ErrorFile"/>
        </Root>
    </Loggers>
</Configuration>
